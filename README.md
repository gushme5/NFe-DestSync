# NFe-DestSync
NFe DestSync is a Windows automation tool that converts Brazilian NF-e XML files before ERP systems import. It validates recipients by CNPJ, safely replaces only the &lt;dest> block using XML DOM/XPath, renames files, moves originals to a processed folder, and generates CSV logs.
